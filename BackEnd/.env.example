# Copie este arquivo para .env e ajuste se precisar.
# Os valores abaixo já batem com o docker-compose.yml deste diretório.

# Servidor Express
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=remedios
DB_PASSWORD=remedios
DB_NAME=remedios_db
